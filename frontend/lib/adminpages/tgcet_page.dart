import 'package:flutter/material.dart';

class TgcetPage extends StatelessWidget {
  const TgcetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("TGCET Page")));
  }
}
