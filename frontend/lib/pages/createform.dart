import 'package:flutter/material.dart';

class CreateForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create Form')),
      body: Center(child: Text('Form Creation Page')),
    );
  }
}
